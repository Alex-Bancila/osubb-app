# OSUBB App

Internal application for OSUBB (Organizația Studenților din Universitatea Babeș-Bolyai, Cluj): Task Tracker + points, Calendar/RSVP, announcements, notifications, and member/role management. The accepted delivery order is **Task Tracker backend → web foundation → Task Tracker frontend → Calendar → Web Push → Cloudflare production**; readiness is proven by milestone acceptance, not an old date forecast.

**New agent or teammate? Read this file, then `CONTEXT.md`, ADR-0007, ADR-0008, and ADR-0009 before taking work.** Use `docs/agents/onboarding.md` for setup and collaboration conventions, but use the status and issue graph below for the current queue.

## Status (2026-09-22)

- **The Task Tracker backend is complete on `main`,** and so is its frontend's first full pass. Migration and suite counts move daily — run `ls supabase/migrations | wc -l` and `npx supabase test db` for the live numbers rather than trusting a remembered figure. On top of the foundation merged by 2026-09-11 (JWT organization claims; deny-by-default RLS; tasks and points; `my_points`; Calendar/RSVP; announcements; notifications; invite/provisioning; guarded demo data; the Project foundation #268–#274; the Team foundation #276–#280; the Diverse/Secretariat departments #310; the grants-hardening + conventions sweep #363/#365), the **Tracker command wave** (#327–#345 and #296, plan `docs/superpowers/plans/2026-09-14-tracker-command-wave.md`) landed 2026-09-14 → 2026-09-16: the authority kit, the atomic Task commands, `private.evaluate_task` as the **only** place points are computed and written, and the retirement of every legacy write path (#345). CI rebuilds the database, runs all suites and the historical-migration harnesses, checks generated types, Edge Functions and the frontend, and deploys migrations to staging after a merge (`db push --include-all`, so **a migration dated before `main`'s newest applies on staging out of order** — date every new migration after the newest on `main` and rebuild any `create or replace` body from `main`'s latest version). When GitHub Actions is out of monthly quota, `scripts/check-local-ci.sh` mirrors every gate locally, and `scripts/smoke-tracker-commands.sh` runs the end-to-end Tracker smoke test through the public wrappers only.
- **Groups Waves 1–2 are complete on `main`** (ADR-0009; plans `2026-09-18-group-model-wave-1.md`, `2026-09-19-group-model-wave-2.md`; umbrella #505 closed 2026-09-22). Every authority decision — Task read/manage/evaluate, Request decisions, Campaign ownership, Calendar create/update/cancel, leadership metrics — reads Groups. Legacy structure and roster tables are still the write master, mirrored into Groups, and the Origin bridge keeps work `group_id` and the legacy columns consistent until Wave 3 removes both.
- **Wave 3 (#506) is in progress.** On `main` already: the Organization marker (#574); the Member commands `set_member_role`/`set_member_status` with `role_history` and session revoke (#580, #603); `update_task` + `preview_task_update`, one full-state edit until review with an accepted consequence list (#626, ADR-0007 amended 2026-09-21); the Campaign report (#625); `my_capabilities()` and `my_groups()`, which retired the frontend level map (#576); the Tracker, Request picker and Cup reading Groups (#575, #577); the shared Group Task fixture on native rows (#578); and `group_audience()`, which fans Event notifications out to a Group's whole audience, filtered by `can_read_event` (#601). Still open, in dependency order: the origin-bridge drop (#579) → Group structure commands (#582) → roster commands (#583) → Applications (#584), changing a Task's Group (#627), Administrare (#588, #589, #103, #105, #107) → the legacy drops (#585, #586, #587, #590, #591, #593) → closeout (#595). `docs/backend/conventions.md` §10 is binding before touching any bridge or Group consumer.
- **The frontend in `app/` covers the Tracker end to end.** Vite, React 19, TypeScript, React Router, TanStack Query, Tailwind 4 + shadcn/Base UI (Nova), locally bundled Montserrat, authentication/route guards and a responsive shell. Merged 2026-09-21/22: Dialog and searchable Combobox primitives (#628), the Task form and draft validation on Groups, the direct-Executor picker, queue control, assignment, evaluation, feedback/reopen/cancel dialogs, duplication, Umbrella progress, Request decisions, the member directory with filters, the leadership page, the profile screen, the notification centre, and **"Task nou"** (#639), which was the missing way to create a Task at all. The rating guide is a pop-up whose text is a placeholder until #638 supplies the document. Ionic remains only as leftover migration code in a handful of files. Nothing in `app/` may write a Task table directly — every mutation goes through a command.
- **Not built yet:** notification fan-out for announcements and the notification tables' RLS (#65, #68); Administrare (#588, #589, #103, #105, #107); the rebuilt Calendar frontend; the Web Push provider (#70); production deployment (#77, #78); and Wave 4 — Evaluation Periods, promotions, sanctions and Adunarea Generală eligibility (umbrella #594).
- **Source of truth:** issue bodies and their numeric blockers define assignable work; a closed issue means merged to `main`, not merely "has an open PR". ADR-0007 (amended 2026-09-21: every Task field is editable until review, with accepted consequences; a Campaign is a reporting label, never a filter) defines the Tracker model; ADR-0008 defines Calendar behavior; ADR-0009 generalizes Departments, Teams, Projects and the Adunarea Generală into Groups with settings and Group Roles, amends ADR-0004/0007/0008 by reference, and lands as a three-wave strangler; ADR-0002/0005 define the browser/PWA architecture and rollout. `docs/backend/conventions.md` is binding on every migration — including the lock-mode rule (a parent/Umbrella row is locked `for no key update`, never `for update`) and the error-vocabulary rules. `docs/README.md` indexes every other document as authoritative or historical.

## House rules (must follow)

1. **Never edit the database by hand.** Schema changes are migrations: `npx supabase migration new <name>` → SQL → `npx supabase db reset`. Studio is for looking, not changing.
2. **New tables enable RLS in the migration that creates them.** Deny-by-default; policies come separately.
3. **New views get `with (security_invoker = on)`** — otherwise they run as owner and bypass RLS.
4. **Functions used by triggers/policies:** `security definer` only when needed, always `set search_path = ''` with fully-qualified names. Full backend conventions — command shape, error codes, grants, naming, timestamps, test layout — live in `docs/backend/conventions.md`; `supabase/tests/conventions.test.sql` enforces six of them: empty `search_path` on every security-definer function, no `anon` execute in `public`/`private`, no `authenticated` execute on trigger/`require_*` functions, `security_invoker` views (two named exceptions), no `anon`/`service_role` usage on `private`, and no mention of `groups.category` in any `public`/`private` function body or policy expression outside the three named Wave 1 mirror writers and the `public.my_groups()` projection (#576).
5. **Tests ship with the feature, same PR** (`supabase/tests/*.sql`, pgTAP). A test must fail if the feature is removed. Verify locally: `npx supabase db reset && npx supabase test db` — both green before any PR.
6. **Reference data** (roles, departments, guides, suppression, promotion rules) lives **in migrations**; **demo data** lives in `supabase/seed.sql` (local/staging only, never production). **`db push` does not carry `seed.sql`** — a hosted project only gets demo data from the manual _Seed staging demo data_ workflow, so the file must stay re-runnable against a live database (CI checks this). See `docs/backend/seeding-staging.md`.
7. **One issue = one branch = one PR**, body says `Closes #n`. CI must be green. **Merging is a human act** — never merge or push to `main` directly (docs-only commits to `main` are the exception).
8. **Secrets never in git** — Bitwarden (humans) + GitHub Actions secrets (CI). Set secrets from a real terminal or browser; `gh secret set` through a non-interactive prompt stores an empty value.
9. **Never re-run one-shot scripts**: `scripts/create-github-issues.sh` (historical) and the 2026-08-19 backlog split are done.
10. **Spec Revision 3 supersedes older spec text where they conflict.** Known trap: notification suppression is **bc + bce** (Revision 3 §9.2), not the bc-only insert shown in spec §3.4.
11. Use `CONTEXT.md` vocabulary in code, issues, tests. When output contradicts an ADR (`docs/adr/0001–0008`), surface it — don't silently override.
12. **Every policy on `authenticated` must be unsatisfiable without org claims** — via `auth_level() >= N` (0 without claims) or explicitly via `auth_is_member()`. `to authenticated` is _not_ a membership check: it only excludes `anon`. `auth.uid()` is not one either — a deactivated member keeps their uid and their `profiles` row. The claimless sweep in `rls_deny_by_default.test.sql` enforces this; if it fails naming your table, your policy has an unconditional branch.
13. **ADR-0007, ADR-0008, and ADR-0009 supersede the legacy Task/Calendar model.** Do not add behavior to multi-assignee Tasks, `open`/`overdue` stored statuses, award requests, direct client state changes, or level-4-global Calendar writes. Migrate toward one Executor, Assignment History, Candidate Queue, atomic commands, and Group-Role authority (ADR-0009). Never add a new `dept_id / team_id / project_id` branch to an authority helper — new authority reads Groups.
14. **Respect issue blockers before assignment.** An open issue’s `## Blocked by` list is authoritative. Start it only when every listed issue is closed/merged. `None — can start immediately` means it is safe to take. If code reveals a missing dependency, update the issue before implementation.
15. **GitHub descriptions use real Markdown.** Use `## Goal`/`## What to build`, `## Why`, `## Implementation boundary`, `## Acceptance criteria`, `## Required tests`, and `## Blocked by` as appropriate. Never publish literal `\n` escape sequences or vague blockers such as “the schema issue”; link concrete `#issue` numbers.

## Agent skills

### Issue tracker

Issues are tracked in **GitHub Issues** (via the `gh` CLI) on `Alex-Bancila/osubb-app`. External pull requests are **not** a triage surface. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical triage roles with default strings (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`); `max-1h` is a size label meaning "one focused hour or less". See `docs/agents/triage-labels.md`.

### Domain docs

**Single-context** — `CONTEXT.md` (domain glossary) + `docs/adr/` (ADRs 0001–0009) at the repo root. See `docs/agents/domain.md`. ADR-0007/0008/0009 are authoritative for Tracker/Calendar/Groups. Mandate source documents: `docs/org/`; historical delivery calendar: `docs/roadmap.md`; team operating model: `docs/team/team-plan.md`.

## Backend / local development

The backend is **Supabase** (PostgreSQL + Row-Level Security + Auth + Edge Functions), managed as migrations in `supabase/`. Read ADR-0007/0008/0009 before changing Tracker, Calendar, or Group behavior. The older architecture spec and `docs/backend/implementation-issues.md` provide history, but current GitHub issues and accepted ADRs win when they conflict.

**Prerequisites:** Docker Desktop running; Node. The Supabase CLI runs via `npx supabase` (no global install). Deno only for Edge Functions.

**Why Docker:** `npx supabase start` doesn't install anything on your machine — it starts ~9 Docker containers (Postgres, GoTrue/Auth, PostgREST/Kong, Studio, Mailpit, the edge runtime, …), pre-wired to match the hosted projects exactly. That's what makes "local" a real Supabase stack rather than a simulation, and why `db reset` can rebuild everything from zero in under a minute: containers are disposable, nothing on the host OS is ever touched.

**Run locally:**

- `npx supabase start` — bring up the local stack (first run pulls several GB of images).
- Studio → http://127.0.0.1:54323 · API → http://127.0.0.1:54321 · Mailpit (local email inbox, catches magic links) → http://127.0.0.1:54324
- `npx supabase db reset` — re-apply every migration + `seed.sql`.
- `npx supabase test db` — run all pgTAP suites.
- `npx supabase stop` — shut down.
- **Windows gotcha:** if start/reset fails with "port is not available / access permissions", Windows reserved our ports after a reboot → admin PowerShell: `net stop winnat` then `net start winnat`. If `git ls-files --eol | grep w/crlf` lists files, an editor wrote CRLF into your checkout: restore only those files — `git ls-files --eol | grep w/crlf | cut -f2 | xargs git checkout --` (the tab-delimited second field is the path) — rather than `git checkout -- .`, which would discard any other uncommitted work in the tree. `.gitattributes` (`eol=lf`) is what keeps checkouts LF; `.editorconfig` only keeps editors writing LF going forward.

**Environments:** local (Docker) → **staging** (hosted, auto-updated on merge to `main` via CI) → **production** (Supabase Pro, Spend Cap ON — created in Sprint 3, deploys only via a manually-approved workflow, issue #78). The CI staging job **skipping on PR builds is correct** — deploys happen on merge only.

**Auth is invite-only** (ADR-0003): public sign-up disabled; accounts are created via admin magic-link invite / CSV import; the JWT claims hook stamps role/level/depts/teams/groups; RLS denies anyone without an `activ` `profiles` row. Hosted projects need dashboard config that `config.toml` can't carry (signup off, hook enabled) — see issue #54.
