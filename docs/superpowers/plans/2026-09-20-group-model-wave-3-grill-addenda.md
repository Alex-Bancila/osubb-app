# Group model — Wave 3: grill addenda (2026-09-20, evening)

> Rulings from the second grilling of 2026-09-20 (issue-graph completeness for the Tracker, notifications, and
> the auth process). To be folded into `2026-09-20-group-model-wave-3.md`'s "Execution rulings" by whoever
> holds that file; until then this file is the record. Issue edits and the seven new issues are drafted under
> the session scratchpad (`issues/DRY-RUN.md`) and are applied only after Alex's go-ahead. ADR-0003, 0004,
> 0008, 0009 and `CONTEXT.md` carry the dated amendments already.

| #   | Ruling                                                                                                                                                                                                                                                                                                                                                                           | Lands in                                               |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| D1  | Provisioning by Appointment: `provision_profile(p_group_ids, p_appointed_by)` through #583's shared `private.appoint_group_member(p_group_id, p_member_id, p_actor)`; `invite-member` takes `group_ids`; the CSV keeps `dept,team`, resolved by `groups.short` **or `groups.name`**, case- and diacritic-insensitive.                                                            | new issue G1 (after #583; blocks #590, #107, #112)     |
| D2  | `revoke-sessions` Edge Function: caller level from the DB, refuses unless the target is `inactiv`; #580 stays a pure command (its "existing session-revoke path" is struck); demotion is not revoked (ADR-0003 amendment).                                                                                                                                                       | new issue G2 (after #580; blocks #105)                 |
| D3  | T2 reversed: `hasOwnOrigin` reads `my_groups()`, not the `group_ids` claim. Session refresh on focus when the token is > ~15 min old.                                                                                                                                                                                                                                            | #575 edit; new issue G3                                |
| D4  | Membership Status never edits rosters; inactive targets refused by the roster commands; Administrare roster shows status.                                                                                                                                                                                                                                                        | #580, #583, #588 edits; ADR-0009 amendment             |
| D5  | Moderator is a transferable Role (grant, then removal by the successor; self-target forbidden; Moderator-only for a bc/moderator target, Status included). #112 is the production bootstrap through `provision_profile` with the service key.                                                                                                                                    | #580, #112 edits; ADR-0003/0004 amendments             |
| D6  | **Group Audience** (CONTEXT term): the Group and every Group below, Automatic Membership resolved. `private.group_audience()`; Event recipients and announcement fan-out use it.                                                                                                                                                                                                 | new issue G4 (after #505; before #581); #248/#68 edits |
| D7  | Roster and Role commands notify the target (`system`, direct, never the actor); `set_member_status` writes none. Member-facing `/grupuri/:id` is the link target (R8 gains the route).                                                                                                                                                                                           | #580, #583, #589 edits                                 |
| D8  | #101 rewritten as startable now (list, badge, mark-read, mark-all-read under #65's policy). Realtime limited to own Notification rows, invalidation only.                                                                                                                                                                                                                        | #101 rewrite; new issue G5                             |
| D9  | Announcements: `group_id` = Origin + `audience` (`local`/`org`); per-Origin compose gate (`can_manage_group_work(group_id)` or Organization Group + any Group Role); the BCE-by-rank arm is dropped (R7 corrected). `my_groups()` returns effective Group Roles (descendants of managed Groups) + `explicit`. No new capability column.                                          | #581, #576, #100 edits; CONTEXT terms                  |
| D10 | Login accepts the emailed six-digit code (`verifyOtp`); templates carry `{{ .Token }}`; #54/#77 checklists.                                                                                                                                                                                                                                                                      | new issue G6; #54/#77 edits; ADR-0003 amendment        |
| D11 | `archive_group` refuses `PT409 group_has_open_work` (non-terminal Task or pending Request in the subtree); declines pending Applications and cancels future Events itself.                                                                                                                                                                                                       | #582 edit; ADR-0009/0008 amendments                    |
| D12 | **No `move_group`** — "we will never move a group". Parent fixed at creation; `validate_group_path` UPDATE arm and `cascade_group_path` stay as tripwires; conventions assert no function updates `groups.parent_id`. Decision 5's "parent (re-parenting…)" and the cross-cutting "one re-parent per statement" are struck. Cup attribution stays live with nothing ever moving. | #582 edit; ADR-0009 amendment; CONTEXT Child Group     |
| D13 | Tracker: convert mode/Audience before participation — the one ADR-0007 command with no screen.                                                                                                                                                                                                                                                                                   | new issue G7 (after #184; child of #90)                |
| D14 | Demotion below a Group's Minimum Level removes the Member from that Group (rows and Group Roles on it; ancestor rows untouched; pending Applications withdrawn; notification names the Groups left). The one case where a Role change edits rosters.                                                                                                                             | #580 edit; ADR-0009/0004 amendments                    |
| D15 | Raising a Minimum Level above current members: `PT409 group_has_members_below_level` unless `p_confirm_removals`; then removal + notifications (members and Managers). Own roster only (children already refused by `group_min_level_above_children`). The `23514` invariant (#586) is total; R17's self-limb exception is struck.                                               | #582, #588, #586 edits; ADR-0009 amendment             |

**Roster deltas changed by the above:** #582 roster +5, public +4 (was +6/+5); #583 roster +4, public +3 (the shared core); G4 roster +1.

**Plan text to strike:** Decision 5 "parent (re-parenting, one row per statement)"; cross-cutting "one re-parent per statement" and "the re-parent race"; T7 "calls the existing session-revoke path"; T8's gate "(caller_level() >= 5 or …)"; R7's "or rank BCE+"; R17's second sentence; T9's `move_group` row; the Verification line "no function … mentions …" gains `parent_id` writes.

**Checked and unchanged:** #70 (push decision, its ADR files the children), #66/#69 (PRs open), #204, #584, #585, #587, #591–#595, Wave 4 (#594 and its children).
