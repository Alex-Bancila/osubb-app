# Production readiness — grill rulings (2026-09-23)

> Rulings from the grilling of 2026-09-23 on the per-page changes, the remaining waves, and the issue
> graph that takes the app to production. Issues, labels, and milestones are applied to GitHub only after
> Alex's go-ahead. Glossary changes land in `CONTEXT.md` as each term is resolved.

## Facts established before the rulings

- Event creation from the agenda is on `main` (#661, 2026-09-23). The Application commands are on `main`
  (#584, 2026-09-23); their UI is #589, open. Hiding the Request form from BCE+ is #631, filed.
- `express_task_interest_impl` makes the first eligible Member the Executor (`first_come`), later Members
  queue; `give_up_task` and `update_task` promote the oldest pending Candidate automatically.
- `announcement_reads` exists (self-only policy); read state does not affect order; the unread count is on
  the page header only. Announcements are still written directly under `auth_level() >= 4` (#581 open).
- Notifications: "Marchează tot ca citit" is a direct update at `NotificationsScreen.tsx:126-133`.
- Acasă: greeting, `MyPointsCard`, and for `seeLeadership` the Leaderboard and Cup cards. No tasks/events.
- Taskuri: member tabs use `TaskCard` (one column); manager tabs use a 7-column horizontally scrolling table
  with Origine / Stare / Campanie / title search. Disponibile already shows org-audience Opportunities of
  other Groups, sorted own Groups first, without a visual distinction.
- `tasks.description` is plain text; `submit_task_for_review(p_task_id)` takes no note; no markdown or
  link handling anywhere; no validation library; no date-range filter anywhere.
- `profiles` has no nickname column; `MemberProfileDialog` exists and is used in two places; names are
  otherwise plain strings or private one-off components.
- Voluntari shows up to three Group chips + "+N"; Profil edits `full_name`, `phone`, `avatar_color` directly.
- Calendar is an agenda list, upcoming only, no month grid, no filters.
- Milestones already exist per wave (Wave 1–4) plus eleven old Epic milestones with stragglers.

## Rulings

| #  | Ruling | Lands in |
| -- | ------ | -------- |
| R1 | Every remaining issue is filed now with real blockers; backend children before frontend children; CI green on every PR, no relaxed gate; a backend PR that changes a command the app calls carries the minimal call-site fix. | issue graph |
| R2 | Labels: `wave-3`..`wave-6` (one per issue, mirrored by a milestone; Epic milestones close); twelve `page-*` labels (acasa, taskuri, clasament, cereri, campanii, calendar, anunturi, notificari, voluntari, profil, grupuri, administrare); `shared` for the cross-cutting kit. A backend issue takes the page it serves. | labels |
| R3 | **Work Filter** (glossary): root Group → Group below → Campaign → date range; Group means the Group and everything below; Campaign options are those able to tag a Task in the chosen Group (owned on its path or below); date = Task deadline, Event start, points award date; state in the URL. | `CONTEXT.md` (done); shared issue |
| R4 | Acasă keeps its leadership cards for BC/BCE as it is today. Everyone gets the next Task by deadline and the next Event, each a deep link that opens the page filtered and scrolled to the row. | page-acasa |
| R5 | **Nickname** (glossary): nullable `profiles.nickname`, self-editable and BC/Moderator-editable; 2–24 chars of letters, digits, spaces, `.`, `-`, `_`; trimmed before validation (never rejected for edge whitespace); unique across all profiles ignoring case and diacritics; empty = full name shown. Every list, card, leaderboard, notification and history line shows the Nickname (server-side notification text reads it at write time); Voluntari and Administrare show the full name underneath. `full_name` becomes a privileged column (BC/Moderator only), reversing R15 of 2026-09-21. | shared; page-profil; page-administrare |
| R6 | **Member Card** (glossary): the pop-up behind every name — avatar, Nickname, full name, Role, "Membru din", Groups as first Department + n. Contact only for viewers who can read `profiles_contact` (level ≥ 5). Never points or rank. BC/Moderator get an "Editează" link into the Administrare member page (#103); leadership viewers get a link to the Member's tracker. One read-only server function returns the projection so ordinary Members can see a colleague's Groups. General rule: every text input is trimmed before validation. | shared |
| R7 | **Attached Link** and **Submission Note** (glossary). Text stays plain (no Markdown, no inline URL detection). A Task carries at most one Attached Link (`tasks.link_label` + `tasks.link_url`, set together, http(s) only, same shape as `announcements.form_label`/`form_url`), editable through `create_task`/`update_task`. `submit_task_for_review(p_task_id, p_note default null, p_link_label default null, p_link_url default null)` writes the Submission Note on the submission's history row (`task_activity.note` + link in `details`); the details sheet and the reviewer's card show the latest one. "Send it to feedback" in the ask means Submit for review. | page-taskuri (backend + frontend) |
| R8 | **Constraints kit.** Server is the source of truth: a check constraint or command check per rule, each raising a stable reason code (`title_too_long`, `deadline_in_past`, …), one mutation test per rule. Frontend: one zod schema per entity mirroring the limits, one shared reason→Romanian table (the six per-feature tables fold into `command-reasons.ts`), validation on blur and submit, the broken rule shown under the field, server reasons landing under the same field. Normalise before validating: trim ends, lowercase email, phone → E.164. Limits: titles 3–120; Task description and Announcement body ≤ 2000; notes/reasons ≤ 1000; Nickname 2–24; link label ≤ 60, address http(s) ≤ 2048; deadline / Event start not in the past at creation, Event end after start; date range end ≥ start; Difficulty/Rating 1–5; capacity 1–1000. **Phone:** strip spaces, dots, dashes, parentheses; leading `00` → `+`; no prefix → `+40`; after `+40` or `+373` drop a trunk `0` (`+40 0730655145`, `0730655145`, `+40730655145` → `+40730655145`; `+373 069…` → `+37369…`); RO mobile = `+40` + 9 digits starting with 7, MD = `+373` + 8 digits; any other explicit `+<country>` accepted, E.164 length only (8–15 digits). | shared |
| R9 | **No first-come Executor.** `express_task_interest` always enqueues; `first_come` is retired; `give_up_task` and `update_task` never promote a Candidate — the Task returns to `todo` with its queue intact and the managers are notified; `select_task_candidate` is the only way an Executor is chosen on a public Task. Glossary Candidate Queue rewritten; ADR-0007 amended 2026-09-23. Card copy: always "Te-ai înscris pe locul N". | `CONTEXT.md`, ADR-0007 (done); page-taskuri backend + frontend |
| R10 | **Disponibile bands.** Upper band: Opportunities of the Member's own Groups in the Group colour; Organization Group items OSUBB red in the same band. Lower band, greyed: **Other OSUBB Opportunities** — every open Opportunity (public, queue open, non-terminal) of a Group the Member is not in but whose Minimum Level they satisfy, any Audience; local-Audience ones show "doar pentru membrii grupului" and no join button. `can_read_task` widens accordingly (ADR-0007 amended, `CONTEXT.md` Task Audience reworded). Both bands in deadline order under the Work Filter. Taskurile mele: Personal Score header + rebuilt card (Group stripe, chips, deadline/overdue/late badge, status, Executor as Member Card, queue position, Attached Link). De gestionat / Toate: same card in dense row form, no horizontal scroll; Stare + title search + Work Filter; overdue pinned first. Calendar mirrors: coloured own Groups + OSUBB red org Events, grey Other OSUBB Events (already the rule). | page-taskuri; page-calendar |
| R11 | **Clasament.** Rows are Voluntari-style (avatar, Nickname, first Department + n, points), each a Member Card button whose main link opens the Member's tracker. Work Filter replaces the two selects; date range = award date. Ranking counts the Task's Group, never the Member's Groups (ADR-0007 unchanged); outsiders who earned points there appear, with their Department chip showing it. The Member tracker page gets the new cards and the Work Filter (deadline range). The Cup block keeps its rule and takes the date range. Gate unchanged (`seeLeadership`, level ≥ 5). | page-clasament |
| R12 | **Cereri.** #631 stands: at level ≥ 5 the submission form and "Cererile mele" are hidden, the decision queue stays, route and nav item stay for everyone (a level-1 Coordonator Principal decides). Requester names in the queue become Member Card buttons. | page-cereri |
| R13 | **Campanii.** Manager-only page keeps its gate. Work Filter without its Campaign level: root Group → Group below, options limited to the Groups the caller manages; the list shows the Campaigns owned by the chosen Group and every Group below it, each row naming its owner. Active/inactive toggle (default active). Date range applies to the Campaign report (points awarded inside the range). Contributor names become Member Card buttons. A Campaign stays a label, never an Origin or a who-may-work filter. | page-campanii |
| R14 | **Calendar.** Two views, Lună and Agendă (toggle remembered per device). Grid chips: readable Events + the Member's own Task deadlines (Executor or pending Candidate); manager toggle "Taskurile gestionate" adds managed Tasks; tapping a day lists it below. Work Filter incl. Campaign level, date range on Event start. Past Events readable under the same Minimum Level rule (ADR-0008 amended). `events.campaign_id` nullable, same validity rule as Tasks (owned on the Event's Group path), set by `create_event`/`update_event`, not an important change; glossary Campaign now tags Tasks and Events, points only via Tasks. Acasă deep link opens the Agendă scrolled to the Event. | page-calendar (backend + frontend) |
| R15 | **Anunțuri.** Order: pinned first (any read state), then unread newest first, then read newest first. Badge = unread announcements the Member may read, on the nav item and the mobile tab, same shape as the notifications badge, refreshed by #604's hook. Readers: one read-only function per announcement returning its audience with read/unread + time, and a "citit de x din y" line; visible to the author, the Origin's Managers/Responsibles (ancestors included), BC/Moderator; for organization-wide announcements only the author and BC/Moderator. Priority (normal/important/critical) stays exactly as it is, including the critical banner. Author and reader names are Member Card buttons. Compose (#100) inherits the Attached Link and the constraints kit. | page-anunturi (backend + frontend), after #581 |
| R16 | **Notificări.** Remove "Marchează tot ca citit"; a notification is read only when opened. Badge unchanged. | page-notificari |
| R17 | **Voluntari.** One chip = the Member's earliest-joined top-level Group (by roster `created_at`; the initial Department for every provisioned Member), "+n" = their other explicit memberships excluding the Organization Group and archived Groups; both open the Member Card. No server function reads `groups.category`. The same projection function feeds the Member Card, the Voluntari card and the Clasament row. | page-voluntari; shared |
| R18 | **Profil joining card.** "Period open" = the Group's own `accepts_applications` toggle; no org-wide period. A Group gains an optional Attached Link "Formular de înscriere" (`groups.application_form_label/_url`): when set, "Aplică" opens the link and the Group's Responsibles Appoint from the answers; when empty, the in-app Application (#584/#589) applies. Setting, not a category rule. Profil below level 5 shows "Grupurile mele" + pending Applications + a button to `/grupuri` (page stays, label `page-grupuri`); absent at level ≥ 5. Recrut rule unchanged (Department Application Level 1). Phone self-editable; email via #632; full name per R5; Nickname editable. Glossary Application amended. | page-profil; page-grupuri; page-administrare (setting) |
| R19 | **Wave 3 tail.** Serial backend lane: #581 → #585 → #586 → #587 → #590 → #591 → #593 → #595, #602 beside; #592 stays Alex's staging step before #593 deploys. #590's blockers become #581, #587, #602 (remove #589, #103, #105, #107, #588 — the app reads no legacy table; the panels do not exist yet). #613 closes into #610; #6 closes (children merged); #203 closes (superseded by the ADR amendments); #67 keeps only the final cross-role sweep, startable; #621/#622 startable; #604 moves to Wave 5 / page-notificari, startable. Ionic leftovers fold into the Calendar, Acasă and Profil rebuilds plus one `shared` issue for `App.tsx`, `main.tsx`, `components/states`. | wave-3 |
| R20 | **Wave 4 additions.** Three new issues: Period commands (`open_evaluation_period` / `close_evaluation_period`, BC/Moderator, one open Period, close stamps the threshold and runs #52's close-time promotions + Retention Signals atomically); Organization settings (key-value table, BC-writable through one command, member-readable; first key = adherence-form link read by the promotion notification); the "Perioade de evaluare" Administrare panel (open, close, seed the initial threshold, Retention Signals list with a link into #105's role editor). Seed placeholders BC ratifies: Recrut→Voluntar 6 months; Voluntar→Voluntar Activ 6 months; x = 30%; y = 25%; initial threshold 30. **Sanctions (#104 and its split) leave every wave**: label `after-launch`, no milestone, to be re-split after the first deployment. | wave-4; after-launch |
| R21 | **Production (Wave 6).** Web Push = Supabase Edge Function with VAPID and the Web Push protocol, a `pg_cron` outbox job (unsent notifications × push tokens), backoff retries, dead tokens dropped; the existing service worker shows the notification and opens its link; documented as ADR-0010; #635 preferences on top. SMTP = Resend from the osubb.ro domain (test domain on staging). Hosting = Cloudflare Pages per ADR-0002/0005: #109/#110 against staging now, production Pages project after #77, #78 gated deploy, #112 bootstrap after #111 and #602. Go-live = Wave 5 accepted on staging; #36 onboarding blocked on the Wave 5 umbrella; dates removed from titles. Human account tasks (#77, #146, #54, #109) start now in parallel. | wave-6 |
| R22 | **Launch scope leftovers.** #629 member photos → `after-launch` (initials stay; the Member Card leaves room for a photo). Rating input becomes a five-star control, Difficulty a 1–5 scale, each step with a short generic Romanian hint (1 "Nelivrat / inacceptabil", 2 "Sub așteptări", 3 "Conform așteptărilor", 4 "Peste așteptări", 5 "Excepțional"); the pop-up shows the same hints, no placeholder text remains; #638 → `after-launch`, `needs-info`, the content swap when the guide arrives. #635 → wave-6 behind push. #37 → the closing issue of wave-6, blocked on #36. #100 stays wave-5 / page-anunturi. | page-taskuri; after-launch; wave-6 |
| R23 | **Lanes.** A (backend, Groups cleanup): #581 → #595 chain, #602, #627, #610, #612. B (backend, pages then Wave 4): shared server side (constraints/reasons, Nickname + member projection, date-range parameters, org settings) → Taskuri commands → Calendar → Anunțuri readers/unread (after #581) → Group form-link setting → Wave 4 (#47, #49, #48, #512, #51, #52, Period commands). C (frontend, shared kit then Taskuri): zod + reasons, Member Card + name buttons, Work Filter + URL state, shell Ionic removal → Taskuri screens → Clasament → Campanii. D (frontend, other pages): #103/#105/#107, #589 + form link, Calendar month view, Anunțuri + #100, Voluntari, Profil, Acasă, Notificări → Perioade panel, #634. Alex: Wave 6 account work (#77, #146, #54, #109, #110, #111), #592, reviews. Three people: A then B on one backend person. | issue graph |
